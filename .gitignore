venv

AGENTS.md
CLAUDE.md

.playwright-mcp/
__pycache__/

*.db-shm
*.db-wal
*.db
*.pyc
*.pdf
*.pka
*.docx

# Extracted quiz images (regenerated by scripts/parse_questions.py)
data/

# Local design/implementation artifacts — working documents only,
# not committed to history.
docs/superpowers/

# Per-machine config — copy `.env.example` to `.env` after cloning.
.env
.env.local

# IDE workspace files (none currently tracked; keeps PyCharm / VS Code
# settings out if anyone opens the repo there).
.vscode/
.idea/
